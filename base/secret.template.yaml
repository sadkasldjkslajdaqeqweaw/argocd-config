apiVersion: v1
kind: Secret
metadata:
  name: ${DB_SECRET_NAME}  # Secret 名称变量
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  username: ${DB_USERNAME}  # 数据库用户名变量
  password: ${DB_PASSWORD_PLACEHOLDER}  # 密码占位符（实际值通过 SealedSecret 加密）